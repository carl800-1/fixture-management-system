# 修改日志 — 夹具管理系统

> 版本号规则：**两位版本号**（1.0、1.1、2.0 …），禁止三位补丁号。
> 每次修改必须递增版本号，并同步以下 **7 处**版本标记：
> 1. `package.json` → `version`
> 2. `package.json` → `description`
> 3. `package.json` → `build.win.artifactName`
> 4. `js/db.js` → `APP_VERSION`
> 5. `main.js` → 文件头注释 + `title`
> 6. `preload.js` → 文件头注释 + `version`
> 7. `index.html` → `<title>`、`brand-name`、`foot-sign`
>
> 有工具校验脚本 `test_render.js`（第 8 节「版本号一致性」）可自动核对以上各处。

---

## v1.9（2026-09-08）

### 优化
1. **关于面板「变更日记」的版本标题去掉日期后缀**：`js/app.js` 的 `aboutHtml()` 中
   `v${log.version} · ${log.date}` 改为 `v${log.version}`——即由 `v1.8 · 2026-09-07` 变为 `v1.8`。
   日期数据仍完整保留在 `CHANGELOG` 中，仅不再显示在标题行，界面更简洁。

### 同步
1. 合并 GitHub 仓库 `carl800-1/fixture-management-system` 的最新改动，保持本地开发源与远端代码一致。

### 测试
- 数据层 test_store.js：**56 项通过**
- 渲染层 test_render.js：**36 项通过**（含 7 处版本号一致性）
- 真实 UI test_e2e.js：**89 项通过**
- 打包产物 check_run.js：**17 项通过**（`NO_SANDBOX=0` 模拟纯双击；关于面板首条已为 v1.9，共 10 个版本）

### 发布
- `D:\project\fixture\v1.9\夹具管理系统.exe`（Electron 31.7.7，已签名）

---

## v1.8（2026-09-07）

### 新增「卡片 / 列表」视图切换（参考备件系统 v2.8 结果列表模式）
1. **夹具列表视图切换**：夹具管理页「夹具列表」面板标题新增「▦ 卡片 / ☰ 列表」切换按钮。
   - 卡片视图：原有卡片网格（默认）。
   - 列表视图：紧凑表格，列 = 编号 / 名称 / 规格 / 分类 / 库位 / 状态 / 累计出库 / 累计回库 / 操作，
     行内嵌操作按钮（出库/送修/回库/维修完成/报废/二维码），整行可点击查看流水。
   - 切换状态经 `state.fixViewMode` 记忆，选中行高亮（`.selected`）在两种视图下均生效。

### 修复
1. **补齐表格样式**：`table-wrap` / `table` / `th` / `td` / 行悬停 / 选中高亮此前**缺失**
   （v1.6 补工具类时遗漏），导致库位明细表无边框、无表头背景、无 hover；本次一并补齐。
2. **「维修完成」按钮类名拼写错误**：`btn-primary` → `primary`（`.btn.primary` 才是有效主色样式），
   主色背景此前一直未生效。

### 测试
- 数据层 test_store.js：**56 项通过**
- 渲染层 test_render.js：**36 项通过**（含 7 处版本号一致性）
- 真实 UI test_e2e.js：**89 项通过**（新增 8.4 列表/卡片切换 7 项）
- 打包产物 check_run.js：**17 项通过**

---

## v1.7（2026-09-07）

### 新增「夹具二维码标签」生成与打印（参考备件系统 v2.16 打印选项与预览）
1. **单夹具二维码**：夹具管理卡片新增「二维码」按钮，点击弹窗展示二维码（`SPMS1|<编号>` 内容）
   及编号 / 名称 / 规格 / 库位信息，支持「下载 PNG」与「打印」。
2. **标签打印**：单个 / 批量打印标签，打印设置面板含
   - 纸张尺寸（A4 / A5 / 小票纸 58mm / 小票纸 80mm / 标签纸 100×150mm / 自定义宽高）
   - 水平对齐（左 / 居中 / 右）、垂直对齐（顶部 / 居中 / 底部）
   - 页边距（上下左右 mm）、份数
   - 设置经 `localStorage` 记忆，下次打开自动恢复
   - **实时打印预览**：按所选纸张比例与对齐方式模拟打印效果（WYSIWYG）。
3. **批量打印标签**：夹具管理页顶部新增「🏷 批量打印标签」按钮，按编号排序批量打印全部夹具标签。
4. **底层改造**：打印由主进程 `webContents.print()` 接管（新增 `print-labels` IPC + `preload.printLabels` 桥接），
   按纸张尺寸（微米）精确送印，配合 `@media print` 的 flex 居中布局，确保不同纸张规格下内容正确居中。
5. **实现细节**：`makeQRToken()` 生成 `SPMS1|<code>`；`drawQR()` 用内置 `qrcode-generator.js` 画二维码；
   `labelCardHTML()` 标签卡片含二维码 + 编号/名称/规格/库位；`#printArea` 仅打印时可见。

### 测试
- 数据层 test_store.js：**56 项通过**
- 渲染层 test_render.js：**36 项通过**（含 7 处版本号一致性）
- 真实 UI test_e2e.js：**82 项通过**（新增 8.3 二维码标签+打印 8 项）
- 打包产物 check_run.js：**17 项通过**

---

## v1.6（2026-09-07）

### 新增「库位管理」模块（参考备件系统 v2.27–v2.32 库位总览）
1. **导航新增「🗺️ 库位管理」入口**（位于「夹具回库」与「流水记录」之间）。
2. **库位总览 10 列网格**：按库位聚合全部夹具，`.loc-grid10`（`grid-template-columns:repeat(10,1fr)`）
   三行可视、纵向滚动浏览；卡片仅显示库位编号（如 `A1-01`），悬停 title 显示件数与不在库（出库/维修中）预警；
   「（未分配）」卡片虚线样式、排序置于末尾。
3. **点击卡片筛选**：点击某库位后，下方表格仅显示该库位夹具明细（编号/名称/规格/分类/库位/状态/累计出库/最近动态），
   当前库位高亮；「清除筛选」恢复全部；点选后纵向滚动位置保持不变。
4. **库位排序**：`natCompare`（数字按数值序，如 A-2 < A-10）+ `locSort`（「（未分配）」置末）。

### 新增「手工盘点」CSV 导出
- 库位管理页「📋 手工盘点（导出 CSV）」按钮：按当前库位（或全部）夹具**逐项明细**导出，
  列 = 编号/名称/规格/分类/库位/状态/累计出库/累计回库/最近动态/盘点人/实盘数量；
  「盘点人」导出时在模态框录入（预填上次值、取消即中止），非原生 `prompt`。

### 编号归一化匹配（参考备件系统 v2.13）
- 新增 `normalizeCode()`（忽略大小写/连字符 `-`/下划线 `_`/空格 + 剥离二维码前缀）与 `codeLike()`（普通包含 + 归一化比较）。
- 接入四处编号匹配：夹具管理搜索、出库/回库四栏互查的编号栏、流水记录搜索，
  扫描枪录入 `20-01`、`20_02`、`SPMS1|2003` 均能命中 `2001`/`2002`/`2003`。

### 样式补齐
- 补齐 `table-wrap` / `badge` / `tip` / `spacer` / `ml` / `mt` 等工具类样式（此前多处引用但未定义，导致列表/徽章/提示样式缺失）。

### 测试
- 数据层 test_store.js：**56 项通过**
- 渲染层 test_render.js：**36 项通过**（含 7 处版本号一致性；导航视图映射改为从源码自动推导）
- 真实 UI test_e2e.js：**74 项通过**（新增 8.1 库位管理、8.2 编号归一化两组共 13 项）

---

## v1.5（2026-09-07）

### 界面调整（与备件仓库管理系统 v2.32 风格对齐）
1. **移除底部状态栏**
   - 删除 v1.4 新增的 `<footer class="app-footer">`（含 `v1.4 · 本地文件（JSON）`、
     `design by Frank, 2026.7.27` 与「关于 / About」文字按钮），主区域回归「顶栏 + 内容」两段结构，
     与备件系统 v2.32 布局完全一致。
2. **侧边栏底部署名替换**
   - 原第 1 行「本地存储 · 无需联网」→ 改为 `夹具管理系统 v1.5`（版本标识）；
   - 原第 2 行「夹具管理系统 v1.4」→ 改为 `design by Frank, 2026.7`（用户指定文案）。
   - 样式同步备件系统：`.sidebar-foot` padding 12px 18px、字号 12px、颜色 `#64748b`；
     `.foot-sign` 独占一行、上间距 4px、透明度 .85。
3. **「关于 / About」改为右下角蓝色浮动胶囊按钮**
   - `index.html` 新增 `<button id="aboutBtn" class="about-btn" onclick="openAbout()">`，
     CSS 为 `position:fixed; right:18px; bottom:18px; z-index:40`，背景 `var(--primary)` 品牌蓝、
     圆角 999px、带 `0 4px 14px rgba(37,99,235,.35)` 阴影，悬停上浮 1px 并加深为 `--primary-dark`。
   - 函数名由 `showAbout` 改为 `openAbout`（保留 `window.showAbout` 别名兼容旧 onclick）。
4. **关于面板改为「变更日记」滚动结构**
   - 采用备件系统同款 `.about-panel` / `.about-scroll` / `.about-h` / `.about-ver` / `.about-list` 结构：
     标题「变更日记」+ 各版本 `<h4 class="about-ver">vX.Y · 日期</h4>` + 条目列表，
     底部一行版本信息 `当前版本：v1.5 · 数据文件：程序所在目录 fixture-data.json · 本地存储 · 无需联网`。
   - 移除面板内的「关闭」按钮，改用模态框右上角 × 关闭。
5. **补齐 `.muted` 样式类**
   - 夹具系统此前多处使用 `class="muted"` 但未定义该样式（文字不变灰），
     现补 `.muted { color: var(--muted); }`，与备件系统一致。

### 测试
- 数据层 test_store.js：**56 项通过**
- 渲染层 test_render.js：**34 项通过**（含 7 处版本号一致性）
- 真实 UI test_e2e.js：**62 项通过**
- 打包产物 check_run.js：**新增 12 项关于按钮/署名检查**

---

## v1.4（2026-09-07）

### 新增功能
1. **底部状态栏 + 关于/About 面板**
   - 主界面底部固定状态栏：左侧显示版本号 `v1.4 · 本地文件（JSON）`、
     `design by Frank, 2026.7.27`，右侧放置「关于 / About」按钮。
   - 点击「关于 / About」弹出模态框，展示夹具管理系统各版本变更摘要。

### 优化
2. **出库/回库「四栏互查」**
   - 出库页与回库页顶部改为四个输入框：编号 / 名称 / 规格 / 库位。
   - 任一框输入即实时检索（AND 条件），命中唯一时自动回填并弹出操作表单；
     命中多条时展示候选列表，点击选择。
   - 编号框支持 `SPMS1|2001` 二维码前缀（change 事件自动解析）。
   - 新增「清除已选内容」按钮，一键清空四栏与候选列表。
3. **记录查询兼容二维码前缀**
   - 流水记录搜索框输入 `SPMS1|2001` / `SPMS|2001` 时，先剥离前缀再匹配编号。
4. **中文输入法守卫**
   - 夹具管理搜索框、出库/回库四栏、记录查询搜索框均增加 `compositionstart/compositionend` 监听，
     中文组合输入期间不打断、不触发搜索，解决中文输入被截断/重复的问题。
5. **夹具管理搜索支持库位与分类**
   - 搜索框除编号/名称/规格外，额外支持库位、分类字段过滤。

### 版本标记
7 处版本标记已同步为 v1.4（`test_render.js` 第 8 节「版本号一致性」校验）。

---

## v1.3（2026-09-07）

### 缺陷修复
1. **启动后弹出一个标题为「标题」的空白模态框，无法操作（严重）**
   - **现象**：程序启动后，页面中央弹出一个标题为「标题」、内容空白的模态框，
     遮挡整个界面，点击关闭按钮或 ESC 后才能继续操作。
   - **根因**：CSS 中 `.modal-overlay { display: flex; ... }` 的优先级高于 HTML 的 `hidden` 属性。
     默认状态下 `<div id="modalOverlay" hidden>` 虽然 `hidden=true`，
     但计算样式仍为 `display: flex`，导致模态框默认可见。
   - **修复**：在 CSS 全局增加 `[hidden] { display: none !important; }`，
     确保所有带 `hidden` 属性的元素默认不显示。
   - **验证**：打包产物启动时 CDP 实测 `modalOverlay.hidden === true` 且
     `getComputedStyle(display) === 'none'`。

---

## v1.2（2026-09-07）

### 缺陷修复
1. **窗口只剩标题框、无法关闭也无法切换（严重，导致程序完全不可用）**
   - **现象**：双击 exe 后程序启动，但只出现一个标题框，界面空白，无法关闭也无法切换到其它窗口。
   - **根因**：受限环境（企业电脑 / 远程桌面 / 虚拟机 / 沙箱）下 **Chromium 沙箱无法初始化**，
     GPU 进程反复崩溃，累计后触发
     `FATAL: gpu_data_manager_impl_private.cc(449) GPU process isn't usable. Goodbye.`，
     程序以退出码 3 直接终止，只剩一个空标题框。
   - **排查**：源码中并无任何 `alert/confirm/prompt/dialog` 调用（排除原生弹窗）；
     逐一实测启动开关后确认——
     | 开关 | 结果 |
     |------|------|
     | `--disable-gpu` | ❌ 仍 FATAL 崩溃（退出码 3） |
     | `--disable-gpu --disable-software-rasterizer` | ❌ 仍 FATAL 崩溃（退出码 3） |
     | `--no-sandbox` | ✅ 正常启动并持续运行 |
     说明根因是**沙箱**而非 GPU，`disableHardwareAcceleration()` 亦无法阻止 GPU 进程启动。
   - **修复**：`main.js` 中 `app.commandLine.appendSwitch('no-sandbox')` + `appendSwitch('disable-gpu')`，
     并用 `if (!app.isReady())` 保护 `disableHardwareAcceleration()`（该方法只能在 app ready 前调用，
     否则 `test_e2e.js` require 主进程模块时会抛
     `app.disableHardwareAcceleration() can only be called before app is ready`）。
   - **说明**：本程序为纯本地应用、不加载任何远程网页内容，关闭沙箱的兼容性收益大于安全代价。
2. **修复已内置到程序**：不再依赖外部命令行参数，双击 exe 即自动生效。

### 版本标记
7 处版本标记已同步为 v1.2（`test_render.js` 第 8 节「版本号一致性」34 项校验全过）：
`package.json`(version/description/artifactName) + `js/db.js`(APP_VERSION) + `main.js`(标题) +
`preload.js`(version) + `index.html`(title/brand-name/foot-sign)。

### 测试
- `test_store.js` 数据层：**56 项通过**
- `test_render.js` 渲染层 + 版本号一致性：**34 项通过**
- `test_e2e.js` 真实 UI：**62 项通过**
- 打包产物（模拟用户双击、不加任何开关）：**5 项通过**——窗口标题 v1.2、
  页面渲染 287 字符（非白屏）、导航项 6 个、`showView` 已挂载、无 GPU 致命错误

### 发布
输出目录 `D:\project\fixture\v1.2\`（exe 与数据文件同目录）。v1.1 目录保留可回滚。

---

## v1.1（2026-09-04）

### 交付调整（2026-09-04 追加）
- **移除便携单文件版**：`portable` 目标产物是 NSIS 自解压壳，双击先弹解压对话框（用户误以为「不能运行」），
  且数据写临时目录、换机即丢。已删除 `夹具管理系统_v1.1.exe`，打包 target 改为仅 `dir`。
- **正式交付为解包目录版**：`release_v11b/win-unpacked/夹具管理系统.exe`，
  双击直接打开主窗口，数据在同目录 `fixture-data.json` 落盘。
- **输出目录重定向到 `D:\project\fixture`**：`directories.output` 改为 `D:/project/fixture`（源码 + 构建目录两处同步），
  每个版本一个以版本号命名的子文件夹（`v1.1` / `v1.2` …），发布时把构建产物 `win-unpacked` 重命名为对应版本目录。
- 本次为交付物整理与打包配置调整，程序功能/代码未变，版本号保持 v1.1。

### 新增功能
1. **扫码集成** — 出库 / 回库页面嵌入摄像头扫码区（`#fix_scan_out_area` / `#fix_scan_return_area`），
   复用 jsQR 解码，识别 `SPMS1|<code>` 二维码后直接弹出对应操作表单。
2. **批量扫码出库** — 新增「批量扫码」入口，连续扫码多个夹具逐个自动出库，
   实时累计成功数量并显示于 `#fix_scan_batch_tip`；非在库夹具自动跳过并提示。
3. **维修记录表单** — 新增送修流程，表单含维修类型选择（普通维修 / 校准 / 更换配件 / 保养）、
   经办人、送修说明字段。
4. **维修完成回库** — 维修中（repair）的夹具可标记维修完成，直接回到在库（stocked）状态。

### 缺陷修复
1. **维修完成功能完全不可用（严重）** — `fixtureRepairComplete` 提交的是
   `type: 'return'`，但数据层规定 `return` 只能作用于 `checked_out` 状态；
   维修中的夹具状态是 `repair`，因此该操作**必然抛「夹具未处于已出库状态」**，
   夹具永远卡在维修中。
   **修复**：新增独立流水类型 `repair_done`（repair → stocked），
   数据层增加校验与状态迁移，`FIX_TX_TYPE` 增加 `✅ 维修完成` 显示项。
2. **维修子类型丢失** — 送修表单选的「校准 / 更换配件 / 保养」原被塞进 `remark`
   （`remark: remark || type`），一旦用户填写维修说明，子类型即被丢弃。
   **修复**：新增 `repairType` 字段独立保存，流水记录中可追溯。
3. **清空数据不落盘（严重）** — `clearAllData` 仅清空内存中的 `state`，
   未调用 `DB.clear()`，数据文件原封不动；提示「数据已清空」但重启后数据全部复活。
   **修复**：改为调用 `DB.clear('fixtures')` + `DB.clear('fixTransactions')` 真正落盘，
   并在 E2E 中增加「重启后数据不复活」回归断言。
4. **阻塞式原生弹窗（严重）** — 报废、维修完成使用原生 `confirm()` / `prompt()`，
   样式与出库/回库/送修的 modal 不一致，且在部分环境下会阻塞整个界面。
   **修复**：统一改为项目 modal 风格
   - 报废：新增经办人、报废时间、报废原因字段
   - 维修完成：新增经办人、完成时间、维修说明字段
   - 清空数据：改为输入「清空」二次确认，并展示将删除的夹具数/流水数
5. **回库流程缺失** — 原 `fixtureReturn` / `submitFixtureReturn` 函数完全缺失，
   卡片上的「回库」按钮点击会抛 `ReferenceError`。已补齐完整回库逻辑。
6. **`showView` 未挂载全局（严重）** — `showView()` 定义在 IIFE 内部，
   但「夹具出库」「夹具回库」页面的「返回列表选择」按钮用 `onclick="showView('fixtures')"` 调用，
   点击必然抛 `ReferenceError: showView is not defined`。已改为 `window.showView = showView;`。
7. **构建产物样式丢失（严重）** — 构建目录中 CSS 被放在根目录 `style.css`，
   而 `index.html` 引用的是 `css/style.css`，导致打包后程序无任何样式。
   已修正为 `css/style.css`，并在同步脚本中强制校验。
8. **`parseQRToken` 容错不足** — 原实现无法处理竖线两侧带空格（`SPMS1 | 2001`）
   或输入首尾含空格的扫码结果，返回 `null` 导致识别失败。
   已改为先 `trim()`，再用 `/^SPMS1?\s*\|\s*/i` 剥前缀，并归一化连字符/下划线/空格。
9. **`preload.js` 版本未同步** — v1.0 → v1.1 升级时遗漏，已修正为 `1.1`。

### 测试
新增两套自动化测试（纯 Node，无需 Electron / 摄像头）：

- **`test_store.js`** — 数据层功能测试，**53 项全部通过**
  - 建档（编号唯一性、初始状态、二维码令牌、计数归零）
  - 出库 / 回库状态机与计数
  - **重复出库回库 5 轮循环**（核心需求）验证计数与流水条数
  - 送修 → 维修完成 → 再出库
  - 报废流程与终态校验
  - 异常与边界（不存在夹具、缺省字段）
  - 持久化（JSON 文件结构）与重启恢复（seq 不冲突）

- **`test_render.js`** — 渲染层静态检查，**34 项全部通过**
  - 所有 `onclick` 引用的函数均已挂载 `window`（正是此项揪出 `showView` bug）
  - `DB.*` 方法调用均有实现
  - 所有 IPC 通道均有主进程 handler
  - 状态常量在前后端一致
  - `parseQRToken` **直接执行 app.js 真实源码**验证 10 组输入
  - HTML 静态资源引用全部存在
  - 6 个导航视图均有对应渲染函数
  - **7 处版本号标记一致性**

### 运行方式
```bash
cd fixtures/v1.0
node test_store.js     # 数据层测试
node test_render.js    # 渲染层检查
```

---

## v1.0（2026-09-02）

首个可用版本。

### 核心功能
1. **夹具建档** — 4 位纯数字编号（如 2001），自动分配二维码令牌 `SPMS1|<code>`
2. **出库流程** — stocked → checked_out，记录经办人 / 领用人 / 时间 / 备注
3. **回库流程** — checked_out → stocked（`fixtureReturn` 在本版缺失，于 v1.1 补齐）
4. **报废流程** — 任意状态 → retired（终态）
5. **状态机** — `stocked ↔ checked_out`，`repair` 中间态，`retired` 终态
6. **流水记录** — 每次流转写入 `fixTransactions`，含 `fromStatus` / `toStatus`
7. **统计看板** — 顶部实时显示在库 / 出库 / 维修 / 报废数量
8. **CSV 导出** — 夹具清单导出（含 BOM 头，Excel 中文不乱码）
9. **数据备份** — JSON 导出 / 导入 / 清空
10. **二维码** — 复用 SPMS 格式，jsQR 解码 + qrcode-generator 生成

### 技术架构
- Electron 31 + 原生 HTML/CSS/JS（无前端框架）
- 数据层：Node `fs` 读写本地 JSON（`fixture-data.json`），存放在 exe 同目录
- 进程通信：`contextIsolation: true` + `preload.js` 桥接，IPC 通道 16 个
- 打包：`asar: false` 规避 asar 路径分隔符问题

### 已知问题
- `showView` 未挂载全局（v1.1 修复）
- 构建产物 CSS 路径错误导致样式丢失（v1.1 修复）
- 回库函数缺失（v1.1 修复）
